<% if target.errors.any? %>  
 <div class="errorExplanation">
   <h2>有 <%= target.errors.count %> Lead to the problem could not be submitted at:</h2>
   <ul>
   <% target.errors.full_messages.each do |msg| %>
     <li><%= msg %></li>
   <% end %>
   </ul>
 </div>
<% end %>
